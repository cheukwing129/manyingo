const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const catalogSource = fs.readFileSync(path.join(__dirname, '..', 'public', 'content-catalog.js'), 'utf8');
const pack02Source = fs.readFileSync(path.join(__dirname, '..', 'public', 'question-pack-02.js'), 'utf8');
const pack03Source = fs.readFileSync(path.join(__dirname, '..', 'public', 'question-pack-03.js'), 'utf8');
const lessonPackSource = fs.readFileSync(path.join(__dirname, '..', 'public', 'question-pack-lesson.js'), 'utf8');
const capacityPackSource = fs.readFileSync(path.join(__dirname, '..', 'public', 'question-pack-capacity-01.js'), 'utf8');
const transferPackSource = fs.readFileSync(path.join(__dirname, '..', 'public', 'question-pack-transfer-01.js'), 'utf8');
const transferPack03Source = fs.readFileSync(path.join(__dirname, '..', 'public', 'question-pack-transfer-03.js'), 'utf8');
const transferPack04Source = fs.readFileSync(path.join(__dirname, '..', 'public', 'question-pack-transfer-04.js'), 'utf8');
const transferPack05Source = fs.readFileSync(path.join(__dirname, '..', 'public', 'question-pack-transfer-05.js'), 'utf8');
const transferPack06Source = fs.readFileSync(path.join(__dirname, '..', 'public', 'question-pack-transfer-06.js'), 'utf8');
const transferPack07Source = fs.readFileSync(path.join(__dirname, '..', 'public', 'question-pack-transfer-07.js'), 'utf8');
const fillPackSource = fs.readFileSync(path.join(__dirname, '..', 'public', 'question-pack-fill-01.js'), 'utf8');
const reorderPackSource = fs.readFileSync(path.join(__dirname, '..', 'public', 'question-pack-reorder-01.js'), 'utf8');
const translationOrderPackSource = fs.readFileSync(path.join(__dirname, '..', 'public', 'question-pack-translation-order-01.js'), 'utf8');
function loadContext(){const context={window:{},Map,Set,Array,Object,Number,String,Math};vm.createContext(context);for(const source of [pack02Source,pack03Source,lessonPackSource,capacityPackSource,transferPackSource,transferPack03Source,transferPack04Source,transferPack05Source,transferPack06Source,transferPack07Source,fillPackSource,reorderPackSource,translationOrderPackSource,catalogSource])vm.runInContext(source,context);return context;}
function loadCatalog(){return loadContext().window.ManjingoContent;}
test('reviewed local catalog contains 509 questions across the 89 teachable knowledge points',()=>{const catalog=loadCatalog(),kpIds=catalog.getKnowledgePointIds({teachableOnly:true});assert.equal(catalog.catalogVersion,'reviewed-v1');assert.equal(catalog.questions.length,509);assert.equal(kpIds.length,89);for(const id of ['kp_virtual_wei','kp_read_sentence_core','kp_trans_function_word','kp_transfer_single_sentence','kp_virtual_hu','kp_virtual_qie','kp_virtual_yan','kp_lex_tongjia','kp_lex_intentional','kp_lex_semantic_role','kp_syn_attributive_postpose','kp_syn_fixed_patterns','kp_read_parallel_inference','kp_read_actor_tracking','kp_read_unknown_word_tolerance','kp_trans_fluency','kp_transfer_sentence_pair','kp_transfer_micro_passage','kp_read_argumentation','kp_transfer_short_passage','kp_transfer_mixed'])assert.ok(kpIds.includes(id),`missing ${id}`);});
test('capacity pack keeps only validated explained questions',()=>{const pack=loadContext().window.ManjingoQuestionPackCapacity01;assert.equal(pack.questions.length,28);assert.equal(new Set(pack.questions.map(q=>q.id)).size,28);for(const q of pack.questions){assert.ok(q.kpId&&q.q&&q.a&&q.explanation);if(q.type==='choice'){assert.ok(Array.isArray(q.o));assert.ok(q.o.includes(q.a),`${q.id} answer must appear in options`);}}});
test('transfer packs contain 247 source-aware unseen-context questions',()=>{const w=loadContext().window,legacy=w.ManjingoQuestionPackTransfer01,functionWords=w.ManjingoQuestionPackTransfer03,reading=w.ManjingoQuestionPackTransfer04,translation=w.ManjingoQuestionPackTransfer05,stage=w.ManjingoQuestionPackTransfer06,advanced=w.ManjingoQuestionPackTransfer07;assert.equal(legacy.questions.length,46);assert.equal(functionWords.questions.length,24);assert.equal(reading.questions.length,24);assert.equal(translation.questions.length,24);assert.equal(stage.questions.length,93);assert.equal(stage.knowledgePoints.length,15);assert.equal(advanced.questions.length,36);assert.equal(advanced.knowledgePoints.length,3);const questions=[...legacy.questions,...functionWords.questions,...reading.questions,...translation.questions,...stage.questions,...advanced.questions];assert.equal(new Set(questions.map(q=>q.id)).size,247);assert.equal(questions.filter(q=>/^tr1q\d{3}$/.test(q.id)).length,20);assert.equal(questions.filter(q=>/^tr2q\d{3}$/.test(q.id)).length,26);assert.equal(questions.filter(q=>/^tr3q\d{3}$/.test(q.id)).length,24);assert.equal(questions.filter(q=>/^tr4q\d{3}$/.test(q.id)).length,24);assert.equal(questions.filter(q=>/^tr5q\d{3}$/.test(q.id)).length,24);assert.equal(questions.filter(q=>/^tr6q\d{3}$/.test(q.id)).length,6);assert.equal(questions.filter(q=>/^tr7q\d{3}$/.test(q.id)).length,36);assert.equal(questions.filter(q=>/^tr8q\d{3}$/.test(q.id)).length,21);assert.equal(questions.filter(q=>/^tr9q\d{3}$/.test(q.id)).length,30);assert.equal(questions.filter(q=>/^tr10q\d{3}$/.test(q.id)).length,36);for(const q of questions){assert.match(q.id,/^tr(?:10|[1-9])q\d{3}$/);assert.equal(q.textId,'CROSS');assert.ok(Array.isArray(q.skillIds)&&q.skillIds.length>0,`${q.id}: explicit skillIds required`);assert.ok(String(q.sourceTextId||'').trim(),`${q.id}: real source required`);assert.notEqual(q.sourceTextId,'CROSS');assert.ok(String(q.sourceSentenceId||'').startsWith('sentence:'),`${q.id}: stable sentence id required`);assert.equal(q.sourceKind,'classical-canon');assert.ok(q.transferLevel>=2);assert.ok(q.explanation.length>=12);assert.ok(Array.isArray(q.o)&&q.o.includes(q.a));}});
function assertDedicated(pack,targets,requireRawTiers=false,expectedCount=6,minSources=3){for(const[kpId,skillId]of targets){const questions=pack.questions.filter(q=>q.kpId===kpId);assert.equal(questions.length,expectedCount,`${kpId} should have ${expectedCount} questions`);assert.ok(new Set(questions.map(q=>q.sourceTextId)).size>=minSources,`${kpId} needs at least ${minSources} sources`);assert.equal(questions.every(q=>q.skillIds.includes(skillId)),true);if(requireRawTiers)assert.deepEqual([...new Set(questions.map(q=>q.difficultyTier))].sort(),['application','foundation','transfer']);}}
test('stage 1 function-word pack gives four dedicated skills six questions across diverse sources',()=>assertDedicated(loadContext().window.ManjingoQuestionPackTransfer03,new Map([['kp_virtual_wei','fw.wei'],['kp_virtual_zhe','fw.zhe'],['kp_virtual_suo','fw.suo'],['kp_virtual_ye','fw.ye']])));
test('stage 1 reading pack gives four dedicated skills six questions and three difficulty tiers',()=>assertDedicated(loadContext().window.ManjingoQuestionPackTransfer04,new Map([['kp_read_sentence_core','read.sentence-core'],['kp_read_context_clues','read.context-clues'],['kp_read_logical_relation','read.logical-relation'],['kp_syn_negative_patterns','syn.negative-patterns']]),true));
test('stage 1 translation pack gives four dedicated skills six questions and three difficulty tiers',()=>assertDedicated(loadContext().window.ManjingoQuestionPackTransfer05,new Map([['kp_syn_interrogative_patterns','syn.interrogative-patterns'],['kp_trans_function_word','trans.function-word'],['kp_trans_supplement','trans.supplement'],['kp_trans_ancient_modern','trans.ancient-modern']]),true));
test('transfer 06/07 gives single-sentence plus six stage 2 skills full quality-floor packs',()=>assertDedicated(loadContext().window.ManjingoQuestionPackTransfer06,new Map([['kp_transfer_single_sentence','transfer.single-sentence'],['kp_virtual_hu','fw.hu'],['kp_virtual_qie','fw.qie'],['kp_virtual_yan','fw.yan'],['kp_lex_tongjia','lex.tongjia'],['kp_lex_intentional','lex.intentional'],['kp_lex_semantic_role','lex.semantic-role']]),true));
test('transfer 08 adds three full native skills and three targeted source-diversity questions',()=>{const pack=loadContext().window.ManjingoQuestionPackTransfer06;assertDedicated(pack,new Map([['kp_syn_attributive_postpose','syn.attributive-postpose'],['kp_syn_fixed_patterns','syn.fixed-patterns'],['kp_read_parallel_inference','read.parallel-inference']]),true);const targeted=new Map([['tr8q001',['cy_006','lex.word-class-shift']],['tr8q002',['sx_008','syn.adverbial-postpose']],['tr8q003',['sx_005','syn.ellipsis-object']]]);for(const[id,[kpId,skillId]]of targeted){const q=pack.questions.find(x=>x.id===id);assert.ok(q,`missing ${id}`);assert.equal(q.kpId,kpId);assert.deepEqual(Array.from(q.skillIds),[skillId]);assert.equal(q.sourceKind,'classical-canon');}});
test('transfer 09 closes the final five stage 2 skills with full three-tier packs',()=>assertDedicated(loadContext().window.ManjingoQuestionPackTransfer06,new Map([['kp_read_actor_tracking','read.actor-tracking'],['kp_read_unknown_word_tolerance','read.unknown-word-tolerance'],['kp_trans_fluency','trans.fluency'],['kp_transfer_sentence_pair','transfer.sentence-pair'],['kp_transfer_micro_passage','transfer.micro-passage']]),true));
test('transfer 10 expands three stage 3 passage skills without entering the core schedule',()=>{const pack=loadContext().window.ManjingoQuestionPackTransfer07;assertDedicated(pack,new Map([['kp_read_argumentation','read.argumentation'],['kp_transfer_short_passage','transfer.short-passage'],['kp_transfer_mixed','transfer.mixed']]),true,12,5);for(const q of pack.questions){assert.equal(q.transferLevel,3);assert.ok(String(q.passageId||'').startsWith('passage:'),`${q.id}: passage id required`);assert.ok(String(q.passageText||'').length>=35,`${q.id}: passage text too short`);assert.ok(q.q.startsWith('【閱讀材料】'));}});
test('foundation pack uses stable ids and no retired temporary filler ids',()=>{const pack=loadContext().window.ManjingoQuestionPackLesson,retired=new Set(['lpq001','lpq002','lpq003','lpq004','lpq019','lpq022','lpq023','lpq024','lpq025','lpq026','lpq027','lpq028','lpq029','lpq030','lpq031','lpq032','lpq033','lpq034','lpq035','lpq049','lpq050','lpq051']);assert.equal(pack.kind,'foundation');assert.equal(pack.questions.length,44);assert.equal(pack.questions.some(q=>retired.has(String(q.id))),false);for(const id of ['lpq064','lpq065','lpq066'])assert.ok(pack.questions.some(q=>q.id===id));});
test('all local question ids remain unique',()=>{const ids=loadCatalog().questions.map(q=>String(q.id));assert.equal(new Set(ids).size,ids.length);});
test('every local question points to a known teachable knowledge point',()=>{const c=loadCatalog(),ids=new Set(c.getKnowledgePointIds({teachableOnly:true}));assert.deepEqual(Array.from(c.questions.filter(q=>!ids.has(q.kpId))),[]);});
test('every teachable knowledge point retains at least three curated practice questions',()=>{const c=loadCatalog(),counts=new Map();c.questions.forEach(q=>counts.set(q.kpId,(counts.get(q.kpId)||0)+1));assert.deepEqual(Array.from(c.knowledgePoints.filter(kp=>kp.teachable&&(counts.get(kp.kpId)||0)<3)),[]);});
test('base translation questions are attached to translation rather than sentence-pattern mastery',()=>{const c=loadCatalog();assert.equal(c.questions.find(q=>q.id==='q006').kpId,'kp_translation_001');assert.equal(c.questions.find(q=>q.id==='q010').kpId,'kp_translation_001');});
test('legacy duplicate-looking p3 knowledge points are presented as transfer or synthesis practice',()=>{const labels=new Map(loadCatalog().knowledgePoints.map(kp=>[kp.kpId,kp.content]));assert.equal(labels.get('kp_p3_zhi'),'之：跨語境辨析');assert.equal(labels.get('kp_p3_yi'),'以：跨語境辨析');assert.equal(labels.get('kp_p3_judgment'),'判斷句：跨句辨析');assert.equal(labels.get('kp_p3_translation'),'文言翻譯：綜合策略');});
test('reviewed corrections distinguish the two 其 uses and Cao Gui reasoning',()=>{const c=loadCatalog(),q18=c.questions.find(q=>q.id==='p3q018'),q19=c.questions.find(q=>q.id==='p3q019'),cao=c.questions.find(q=>q.id==='p2q044');assert.equal(q18.a,'反問語氣（難道）');assert.equal(q19.a,'推測語氣（大概／恐怕）');assert.match(cao.a,/小惠未遍/);assert.match(cao.a,/小信未孚/);});
test('question selection follows plan order and selects at most one question per knowledge point task',()=>{const c=loadCatalog(),plan={targetCount:3,items:[{kpId:'kp_caogui_strategy',category:'review',priority:1},{kpId:'kp_yueyang_001',category:'weak',priority:2},{kpId:'kp_p3_translation',category:'new',priority:3}]},q=c.selectQuestionsForPlan(plan,c.questions,10);assert.equal(q.length,3);assert.deepEqual(Array.from(q,x=>x.kpId),['kp_caogui_strategy','kp_yueyang_001','kp_p3_translation']);});
test('misconception review selects the highest-priority previously missed question',()=>{const c=loadCatalog(),q=c.selectQuestionsForPlan({targetCount:1,items:[{kpId:'kp_p3_yi',category:'review',priority:1,misconceptionQuestionIds:['p3q009','p3q010']}]},c.questions,1);assert.equal(q[0].id,'p3q009');assert.equal(q[0].misconceptionReview,true);});
test('multiple questions remain available for runtime rotation when there is no misconception target',()=>{const c=loadCatalog(),candidates=c.questions.filter(q=>q.kpId==='kp_caogui_strategy'),q=c.selectQuestionsForPlan({targetCount:1,items:[{kpId:'kp_caogui_strategy',category:'review',priority:1}]},c.questions,1);assert.ok(candidates.length>1);assert.equal(q.length,1);assert.match(catalogSource,/rotation\.rank\(pool\)/);});
test('planned knowledge points without a question are skipped instead of injecting unrelated questions',()=>{const c=loadCatalog(),q=c.selectQuestionsForPlan({targetCount:2,items:[{kpId:'missing_kp',category:'new',priority:3},{kpId:'sx_001',category:'review',priority:1}]},c.questions,10);assert.equal(q.length,1);assert.equal(q[0].kpId,'sx_001');});
